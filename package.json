{
  "name": "conceptual-definitions-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "node ./scripts/build.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.266",
    "@types/node": "^22.8.4",
    "dotenv": "^16.4.5",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}

